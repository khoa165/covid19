import React from 'react';
import Countries from '../countries/Countries';

const Landing = () => {
  return (
    <div id='landing-page'>
      <Countries />
    </div>
  );
};

export default Landing;
